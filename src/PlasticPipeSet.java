import bagel.Image;

public class PlasticPipeSet extends PipeSet{

    public PlasticPipeSet(Image plastic_pipe, boolean isLevelUp) {
        super(plastic_pipe, isLevelUp);
    }
    public void update(int timeScale){
        super.update(timeScale);
    }
}
